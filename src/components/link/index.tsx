import { Button, Typography, TypographyTooltip } from "@jenesei-software/jenesei-kit-react";
import { createLink } from "@tanstack/react-router";



export const TypographyLink = createLink(Typography);
export const TypographyTooltipLink = createLink(TypographyTooltip);
export const ButtonLink = createLink(Button);
